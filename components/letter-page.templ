package components

import "autojob/models"

templ LetterPage(user models.User) {
	@Layout("Cover Letter", true) {
		<fieldset>
			<legend>Cover Letter</legend>
			<textarea></textarea>
		</fieldset>
	}
}
