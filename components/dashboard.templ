package components

import "autojob/models"

templ Dashboard(user models.User, title string) {
	@Layout(title) {
		<section>
			@AccountNav()
			<h2>{ title }</h2>
			{ children... }
		</section>
	}
}
